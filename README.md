<h1>Huffman Coding</h1>
<h2>Steps to run this project</h2>
<ul>
  <li>clone this repo in a directory say user/foo</li>
  
  <li>inside the repo, run these:
    <h4>cmake -B ./build</h4>
    <h4>Add the <code>input.txt</code> in your build directory</h4>
    <h4>cmake --build ./build</h4>
  </li>
</ul>
<p>The compressed version is stored as <code>input.huff</code> in the build directory after you run the executable</p>
<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/9c2c7e6d-e302-4389-911b-3c8b3441ff3b" />
